import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Menu Bar Groups")
                .font(.title)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
